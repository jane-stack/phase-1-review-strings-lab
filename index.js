const currentUser = 'Jane Nguyen';
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;
const excitedWelcomeMessage = welcomeMessage.toUpperCase(`Welcome to Flatbook, ${currentUser}!`);
const shortGreeting = `Welcome, ${currentUser.slice(0, 1)}!`;
